<%- include("templates/header") %>

<% if (!authorized) { %>
    <h1 class="text-danger text-center mt-5">403 - You are not authorized to access this page.</h1>
<% } else { %>
    <div class="container mt-4">
        <h2 class="mb-4">Admin Page</h2>
        <%- include("templates/user", { users: users }) %>
    </div>
<% } %>

<%- include("templates/footer") %>